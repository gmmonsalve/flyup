import { InputAirportFormatPipe } from './input-airport-format.pipe';

describe('InputAirportFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new InputAirportFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
